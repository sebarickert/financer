#!/bin/bash

docker-compose -f ./docker/docker-compose.production.yml pull webapp
docker-compose -f ./docker/docker-compose.production.yml down
docker-compose -f ./docker/docker-compose.production.yml up -d
